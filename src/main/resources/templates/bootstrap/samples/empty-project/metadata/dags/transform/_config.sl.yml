version: 1
transform:
  default:
  # default configuration for all tasks except those with specific configuration (see below in tasks section)
    writeStrategy:
      type: OVERWRITE
  # uncomment tasks below section to override default configuration for specific tasks
  #tasks:
  #   configuration specific to a task. overrides the default configuration
  #   - domain: kpi
  #     table: order_summary
  #     writeStrategy:
  #       type: OVERWRITE
  #   - domain: kpi
  #     table: revenue_summary
  #     writeStrategy:
  #       type: OVERWRITE

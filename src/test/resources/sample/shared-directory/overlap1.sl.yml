---
version: 1
load:
  name: "OVERLAP1"
  metadata:
    directory: "__SL_TEST_ROOT__/incoming/shared-overlap"
    format: "DSV"
    withHeader: true
    separator: ";"
    writeStrategy:
      type: "APPEND"
